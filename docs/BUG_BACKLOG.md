# LearnLift AI Bug Backlog

| ID | Severity | Area | Issue | Steps to reproduce | Expected result | Actual result | Suggested fix | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| BUG-001 | High | Build / Environment | Debug build appeared blocked by Android SDK license and file permission issues in the Codex shell. | Run `.\gradlew.bat assembleDebug`. | Debug APK builds successfully. | Observed in Codex shell, but local PowerShell / Android Studio build succeeded with `.\gradlew.bat assembleDebug`. | No app fix needed. Keep Android SDK Manager and local permissions healthy; Codex shell access can be revisited later if needed. | Environment-specific |
| BUG-002 | Medium | Emulator / Environment | `adb` is not available on PATH from the Codex shell. | Run `adb devices` from the Codex shell. | Emulator appears as an available device. | Shell cannot find `adb`, but Android Studio Run / local `installDebug` works. | Add Android SDK `platform-tools` to PATH later if shell-based emulator checks are needed. | Environment-specific |
| BUG-003 | Low | Documentation | Testing checklist says to use a real USB device, but current test environment is Android emulator. | Read `docs/TESTING_CHECKLIST.md`. | Checklist includes emulator setup steps. | Checklist is device-focused and does not mention emulator startup/verification. | Add emulator-specific setup and install steps to the checklist. | Fixed |
| BUG-004 | Low | Navigation | Bottom navigation label used `Cards` while feature docs and tests refer to `Flashcards`. | Launch app and inspect bottom navigation. | Tab label matches feature name: `Flashcards`. | Label was `Cards`. | Rename bottom navigation label to `Flashcards`. | Fixed |
| BUG-005 | Medium | Back navigation | Android system Back could exit the app from Settings, Study Path Selection, or Daily Session instead of returning to Home. | Open Settings, Study Path Selection, or Daily Session and press system Back. | App returns to Home. | No explicit Back handler was present for sub-flows. | Add a Back handler that closes the active sub-flow and returns to Home. | Fixed |
| BUG-006 | Medium | Progress | Progress totals are global rather than per study path, which can confuse testing after switching paths. | Select one path, complete cards/quiz, switch to another path, open Progress. | Progress should either be per path or clearly labeled as all-time/global. | Current stats remain global while the selected path card changes. | Either store progress per path or update wording to say all-time device stats. | Fixed |
| BUG-007 | Low | Daily Session | Rapid repeated taps on Known / Needs Review may double-count before the UI moves to the next card. | In Daily Session, reveal a flashcard and tap Known or Needs Review repeatedly very quickly. | One action should be recorded per card. | Risk identified from code review; not emulator-confirmed. | Add per-step answered state or disable buttons immediately after first tap. | Fixed |
| BUG-008 | Low | QA Process | Emulator run was previously blocked from the Codex shell by build/install environment access. | Build and install app on emulator. | App installs and launches on emulator. | Manual local PowerShell verification confirmed `assembleDebug`, `installDebug`, emulator install, and app launch work. | Continue with manual feature checklist. | Fixed |
| BUG-009 | Medium | Future Improvement | Progress is not tracked separately per study path. | Complete activity in one path, switch paths, and review Progress. | A future version may show per-path progress for clearer learning history. | MVP intentionally stores overall local progress only. | Add per-path progress storage and UI after MVP validation if needed. | Deferred |
| BUG-010 | Medium | QA Process | Full manual feature checklist still needs final emulator confirmation. | Run through Home, Study Path Selection, Flashcards, Quiz, Daily Study Session, Progress, Settings, Reset Progress, persistence restart, bottom navigation, and light/dark theme checks on emulator. | Each MVP flow is manually marked Passed or Failed with notes. | Build/install/launch are verified, but the feature-specific checklist has not yet been fully documented as passed. | Complete the emulator checklist and update `docs/QA_INTERNAL_TEST_REPORT.md` with results. | Open |
